{
  "name": "@formatlas/figma-importer",
  "version": "0.1.0",
  "description": "FormAtlas Figma plugin for importing UI dump bundles into Figma layers",
  "private": true,
  "scripts": {
    "build": "BUILD_ENTRY=main vite build && BUILD_ENTRY=ui vite build",
    "dev": "BUILD_ENTRY=main vite build --watch",
    "dev:ui": "BUILD_ENTRY=ui vite build --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.96.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.5.0"
  }
}
